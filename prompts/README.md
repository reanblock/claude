# Prompts

## Install

Copy this `prompts` directory from this repo into your local installation `~/.claude` folder 

Copy the files:

```bash
cp -r prompts ~/.claude/
```

Run claude like so (example uses the rampup system prompt)

```bash
claude --system-prompt-file ~/.claude/prompts/rampup.md
```